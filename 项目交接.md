# HAAndroid 项目交接文档（给下一个 AI Agent）

> 更新日期：2026-08 · 仓库：HAAndroid/（git 主分支 `main`，共 9 个提交，工作区干净，**尚未创建 GitHub 远程**）

---

## 1. 一句话定位

**工业现场边缘视觉感知终端**：一台 Android 设备 = 扫码枪 + 在岗检测 + MQTT 传感上报节点。投递目标公司：宏图智能（工业数智化，命中其「工业图像本地化引擎 / 生产感知」产品线）。这是求职 Demo 项目 A，与其搭配的是 EiAI（工业设备知识库问答助手，RAG）项目 B。

## 2. 当前状态总览

| 维度 | 状态 |
|------|------|
| 功能 | 全部核心链路已实现（条码扫描、人员感知、MQTT 上行/下行、动态策略、离线补报） |
| 编译 | `assembleDebug` / `compileDebugKotlin` 均通过 |
| 真机验证 | ✅ 已验证：MQTT 连接与心跳/检出上报、相机绑定、预览检测框、策略命令链路（回调修复后待复验） |
| ⚠️ 待复验 | 最新两个提交（`125b072` 回调先行、`0b49ef1` 卡死修复）用户尚未在真机复验完 |
| 单元测试 | 无（仅示例测试）；CI 工作流已建未运行（未推 GitHub） |
| 文档 | README.md / API_SPEC.md（v2.0 工业版）/ MQTT_INTEGRATION_GUIDE.md，与代码对齐 |

## 3. 架构与关键文件

```
MainActivity（预览模式）          VisionForegroundService（服务模式，灭屏）
 ├ 相机预览 + OverlayView 检测框     ├ 相机 ImageAnalysis
 ├ 推理协程(Dispatchers.Default)    ├ 心跳(30s)/离线补报(QoS1)/命令处理
 └ 预览 MQTT 上报(5FPS 节流)        └ 策略收集(DataStore Flow)
        │ acquire/release                │ acquire/release
        └─────────► VisionProcessorProvider（共享模型单例+引用计数）◄──┘
                      │
              VisionProcessor（串行流水线：Mutex + 整帧2s超时）
              ├ ① BarcodeScanner（ML Kit standalone，500ms超时，后台executor）
              └ ② FaceRecognizer（MediaPipe 检测 + TFLite MobileFaceNet，GPU→CPU兜底）
                      │
              MqttManager（Paho：回调先于connect；LWT；自动重连；QoS1确认）
                      │
        Room(offline_detections,1000 FIFO)  DataStore(nodeId/broker/fps/策略)
```

| 文件 | 职责 |
|------|------|
| `MainActivity.kt` | 预览模式：相机绑定、检测框、预览 MQTT 上报、启动/停止服务、状态轮询 |
| `service/VisionForegroundService.kt` | 服务模式：FGS(camera)、initCore 分步日志、命令处理、心跳/补报、相机重试绑定 |
| `vision/VisionProcessorProvider.kt` | 模型共享单例 + 引用计数（Activity/服务共用，归零才 release） |
| `vision/VisionProcessor.kt` | 帧流水线：条码先行→人员感知；Mutex；整帧 `withTimeoutOrNull(2000)` 兜底 |
| `vision/BarcodeScanner.kt` | ML Kit 全格式条码；`callbackExecutor` 显式后台线程；close 关 executor |
| `vision/FaceRecognizer.kt` | TFLite MobileFaceNet 192 维 Embedding；GPU delegate `catch(Throwable)` 回退 CPU；模型不可用返回空数组 |
| `vision/OverlayView.kt` | 条码青色框/人员绿色框（后置相机无镜像） |
| `net/MqttManager.kt` | Paho 封装；**回调先于 connect**；`ind/vision/{id}/...` 主题；publishWithCallback(QoS1) |
| `data/SettingsManager.kt` | DataStore：nodeId 落盘、broker 默认 `tcp://192.168.1.2:1883`、fps、检测策略 |
| `data/model/*.kt` | VisionDetectionMessage / Detection / HeartbeatMessage / CommandMessage（kotlinx.serialization） |
| `assets/` | `blaze_face_short_range.tflite`、`mobilefacenet.tflite`（端侧模型） |

## 4. MQTT 协议（与文档三件套一致）

上行 `ind/vision/{node_id}/`：`status`(LWT retained) / `heartbeat`(30s) / `detection`(检出触发,≤5FPS)
下行 `ind/command/#`：`set_detection_policy`（动态开关检测项，持久化）/ `capture_event`（演示桩）/ `send_alert`（通知）/ `update_config`（演示桩）
序列化规则：`encodeDefaults=false` → `is_historical=false` 省略即实时；barcode 的 `confidence` 为 null。

## 5. 构建与环境

- AGP 8.13.2 · Kotlin 2.0.21 · compileSdk 36 · minSdk 24 · Gradle wrapper **9.5.0**（用户本机升级过）
- 关键依赖（版本目录 `gradle/libs.versions.toml`）：CameraX 1.3.4、MediaPipe tasks-vision 0.10.14、TFLite 2.16.1（**含 tensorflow-lite-gpu-api，必须显式声明**）、ML Kit barcode 17.3.0、Paho 1.2.5
- 本机构建：`./gradlew :app:assembleDebug` → `app/build/outputs/apk/debug/app-debug.apk`
- **在 DSH 沙箱内构建的特殊姿势**（重要）：
  - `export GRADLE_USER_HOME="$PWD/.gradle-home"`（写 ~/.gradle 会被沙箱拒绝）
  - `export GRADLE_OPTS="-Dkotlin.compiler.execution.strategy=in-process"`（Kotlin daemon 写 ~/Library 会失败）
  - 签名任务（validateSigningDebug/assembleDebug）需要写 `~/.android/debug.keystore` → 必须以 **danger-full-access** 权限运行；纯编译验证用 `compileDebugKotlin`（无需权限）

## 6. 血泪教训（禁止回退，改代码前必读）

1. **MediaPipe `MPImage.close()` 会 recycle 源位图**（`BitmapImageContainer.close → bitmap.recycle()`，已反编译确认）→ ①传给 MediaPipe 的位图绝不可池化复用；②条码扫描必须在人员检测**之前**消费位图（现顺序：条码→人员）。
2. **TFLite 2.16 拆包**：`GpuDelegate()` 无参构造和 `GpuDelegate.Options` 都引用 `GpuDelegateFactory$Options`，该类在 `tensorflow-lite-gpu-api` 工件中，缺依赖 = 编译过、运行时 `NoClassDefFoundError`。且它是 **Error 不是 Exception**，兜底必须 `catch(Throwable)`。
3. **Paho 回调必须先于 connect() 注册**，否则下行消息（`ind/command/#`）完全收不到——这就是策略命令一度失效的根因。
4. **CameraX 是进程级单例**：Activity 与服务不能同时绑相机。所有权模型 = `VisionForegroundService.isRunning` 标志；点启动服务先 `clearAnalyzer()` 停帧流（非阻塞）→ `unbindAll()` → 500ms 后 startForegroundService；服务端相机延后 400ms + 失败重试 3 次。
5. **推理必须在后台线程**（两处 analyzer 均 `launch(Dispatchers.Default)`）；`bindToLifecycle` 必须在主线程。
6. **ML Kit 回调可能永不返回** → 条码扫描带 500ms 超时、整帧带 2s 超时；否则 Mutex + ImageProxy 被永久占用 → 主线程 `unbindAll()` 死等 → 界面卡死（已发生过的真机故障）。
7. 预览 MQTT 与服务共用 `ind_vision_{nodeId}` clientId，靠 500ms 时序互斥；不要去掉延迟。
8. `processingMutex.isLocked` 快速路径丢帧是设计行为；超时/丢帧日志（`Processing timeout, frame dropped`）属预期。

## 7. 已知待办 / 技术债（按优先级）

- [ ] **P0 真机复验**：`125b072`（策略命令生效）与 `0b49ef1`（启动不卡死）在用户手机上确认；logcat：`adb logcat -s MainActivity:VisionService:MqttManager:VisionProcessor *:S`
- [ ] **P0 推 GitHub**（CI 徽章/工作流已就绪，仓库名暂定 ei8Z/HAAndroid）；首次 CI 运行需验证
- [ ] **P1 录演示视频**：预览模式（检测框+MQTT 同屏）→ 策略命令下发 → 断网补报；脚本在仓库外 `../Demo演示脚本.md`
- [ ] **P2 单元测试**：目前为零（面试硬伤）；可从协议序列化、策略解析、节流逻辑入手
- [ ] **P2 技术债（审查遗留 G 系列）**：无后摄回退提示(G2)、kotlinOptions 弃用(G6)、release 开 R8(G7)、文案入 strings.xml(G8)、魔数提取常量(G9)、重启策略文档(G13)
- [ ] 服务模式无画面检测框（相机所有权设计的代价）；如需两者兼得要引入跨进程 Surface 共享，复杂度高，暂不建议

## 8. 快速验证命令

```bash
# 构建/安装
./gradlew :app:assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk

# 订阅上行
docker exec -it mosquitto mosquitto_sub -t "ind/vision/#" -v

# 下发策略（关闭人员感知，保留条码）
docker exec -it mosquitto mosquitto_pub -t "ind/command/policy" -m '{"command":"set_detection_policy","target":"node_a234823f","parameters":{"enable_person":false,"enable_barcode":true},"timestamp":1752700000000}'

# 日志
adb logcat -s MainActivity:VisionService:MqttManager:VisionProcessor *:S
```

预期日志链（启动服务）：`initCore: start → nodeId → broker → processor ready → connecting MQTT → done` + `MQTT Connected` + `Camera bound (attempt 1)`；策略生效：`Detection policy updated → applied`。

## 9. 上下文背景（与简历/求职的关联）

- 简历文案关键词：工业图像本地化引擎、生产感知、边缘推理、MQTT、动态策略、离线补报
- 面试钩子：条码为什么每 2 帧扫一次（算力预算）、为什么上报 Embedding 而非身份（敏感数据不驻留边缘）、断网怎么办（Room FIFO + QoS1 确认 + 自动重连）、为什么回调先于 connect（Paho 契约）
- 学历短板策略：以「可运行的真机 Demo + 架构文档 + 视频」对冲，不辩解
